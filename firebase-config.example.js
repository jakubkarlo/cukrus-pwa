// Skopiuj ten plik jako firebase-config.js i wypełnij swoimi danymi.
// firebase-config.js jest w .gitignore i nigdy nie trafia do repo.

var FIREBASE_CONFIG = {
  apiKey:            'REPLACE_ME',
  authDomain:        'REPLACE_ME',
  projectId:         'REPLACE_ME',
  storageBucket:     'REPLACE_ME',
  messagingSenderId: 'REPLACE_ME',
  appId:             'REPLACE_ME'
};

// Firebase Console → Project settings → Cloud Messaging → Web Push certificates → Key pair
var VAPID_KEY = 'REPLACE_ME';

// URL twojej appki (bez trailing slash)
var APP_URL = 'https://jakubkarlo.github.io/cukrus-pwa';
